import * as Blockly from 'ngx-blockly/scripts/blockly/typings/blockly';

export class IfBlockLessonModel {
  constructor(public block: Blockly.Block, public color: string, public movement: string) {
  }
}
